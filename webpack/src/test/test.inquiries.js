import assert from 'assert';
import expect from 'chai'
import uploadsInit from '../inquiries';

describe( 'uploadsInit', function (){
  context( 'when the trigger element is not present', function (){
    expect( 'should not throw an error', function (){

    })
  })
  context('when the trigger element is present', function (){
    it(' should add an event listener to the trigger element', function(){

    })

  })
})

describe('showOnClick', function(){
  context('when the trigger element has not been clicked', function(){
    it('the attachments element should be hidden', function(){

      }
    )
  })
  context('when the trigger element is clicked', function(){
    it('the attachments element should be visible', function(){

    })
  })
})
